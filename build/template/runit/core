#!/bin/sh

set -e

# MODIFY THIS
cd /var/src/service-core/components/api-server

export NODE_ENV=production
export NODE_PATH=/var/src/service-core # MODIFY THIS
exec chpst -u app /usr/bin/node src/server --config $APP_CONFIG
